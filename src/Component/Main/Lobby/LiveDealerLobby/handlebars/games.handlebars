<ul class="game-lobby-container clearfix {{activeTabClass}}">
{{#each games}}
    <li class="game-listing">
        <a class="game-list game-listing-item"
        {{#if ../isLogin}}
        data-game-launch="true"
        {{/if}}
        data-game-ugl="{{ ../uglConfig}}"
        {{#if ../uglConfig}}
            {{#equals this.game_provider 'pas'}}
                data-game-provider="ugl"
            {{else}}
                data-game-provider="{{this.game_provider}}"
            {{/equals}}
        {{else}}
            data-game-provider="{{this.game_provider}}"
        {{/if}}
        {{#if this.game_subprovider }}
            data-game-subprovider="{{this.game_subprovider}}"
        {{/if}}
        data-game-code="{{this.game_code}}"
        {{#if this.table_id }}
            data-game-tablename="{{this.table_id}}"
        {{/if}}
        {{#if this.extgame_id }}
            data-game-extgameid="{{this.extgame_id}}"
        {{/if}}
        data-game-platform="{{this.game_platform}}"
        data-game-title="{{this.title}}"
        data-game-target="{{this.target}}"
        data-game-loader="{{this.use_game_loader}}"
        data-game-lobby="{{this.use_game_lobby}}"
        {{#if ../launchViaIframe }}
            data-game-iframe="true"
        {{/if}}
        >
            {{#if this.ribbon}}
                <span class="game-ribbon"
                    style="background: {{this.ribbon.background}}; color: {{this.ribbon.color}};"
                >
                    {{this.ribbon.name}}
                </span>
            {{/if}}
            <img class="w-100" src="{{this.image.url}}" alt="{{this.image.alt}}">
            <p class="label-game-title {{#if this.provider_maintenance}}hidden{{/if}}">{{this.title}}</p>
            
        </a>
        {{#condition this.provider_maintenance "===" true}}
            <div class="game-listing-maintenance">
                <p>{{{../this.configs.games_maintenance_text}}}</p>
                <span class="game-listing-maintenance-title">{{this.title}}</span>
            </div>
        {{/condition}}
    </li>
{{/each}}
</ul>
<div id="game-loader" class="game-loader">
    <span class="game-loader-load-more">Load More</span>
    <div class="sk-fading-circle mobile-game-loader hidden">
        <div class="sk-fading-circle">
            <div class="sk-circle1 sk-circle"></div>
            <div class="sk-circle2 sk-circle"></div>
            <div class="sk-circle3 sk-circle"></div>
            <div class="sk-circle4 sk-circle"></div>
            <div class="sk-circle5 sk-circle"></div>
            <div class="sk-circle6 sk-circle"></div>
            <div class="sk-circle7 sk-circle"></div>
            <div class="sk-circle8 sk-circle"></div>
            <div class="sk-circle9 sk-circle"></div>
            <div class="sk-circle10 sk-circle"></div>
            <div class="sk-circle11 sk-circle"></div>
            <div class="sk-circle12 sk-circle"></div>
        </div>
    </div>
</div>
