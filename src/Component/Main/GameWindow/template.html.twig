<lottoland-app data-version="{{ version }}"
    data-operator-id="{{ operator }}" data-host="{{ host }}"
    data-query-currency="{{ currency }}"
    data-query-locale="{{ locale }}"
    data-query-user-token="{{ token }}">
</lottoland-app>