{% if not is_match %}
    <style>
        html {
            background: black;
            visibility: hidden;
        }
    </style>
{% endif %}

<div class="page-not-found {{ app.product_resolver.getProduct }}" id="page-not-found"></div>
