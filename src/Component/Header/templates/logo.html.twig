{% set logoTag = is_front ? 'h1' : 'p' %}

<{{ logoTag }} class="logo {{ app.product_resolver.getProduct }} {{ app.lang }}">
    {{ include('@component/Header/templates/logo-link.html.twig') }}
</{{ logoTag }}>
